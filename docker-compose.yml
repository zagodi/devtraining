services:
  db:
    build: .
    container_name: devtraining-db
    restart: always
    ports:
      - '5432:5432'
    volumes:
      - .docker/dbdata:/var/lib/postgresql/data
    env_file:
      - .env
